# PRD: Генератор `Report.txt` по Git-выгрузке 1С для MCP

## 1. Название

**1C Configuration Report Generator**

Рабочее имя:

```text
 generate-config-report
```

## 2. Контекст

Используется MCP `CodeMetadataSearchServer` для поиска по коду и метаданным 1С.

Код конфигурации уже хранится в Git в виде выгрузки 1С в XML/BSL. Для работы `metadatasearch` MCP нужен файл `Report.txt`, аналогичный штатному отчету Конфигуратора **“Отчет по конфигурации”**.

Сейчас `Report.txt` можно получить вручную через Конфигуратор. Цель — заменить ручной шаг автоматической генерацией из Git.

Целевая схема:

```text
Git XML/BSL
  ↓
generate-config-report
  ↓
metadata/Report.txt
  ↓
MCP INDEX_METADATA=true
```

Эталонный загруженный отчет показывает, что штатный `Report.txt` — это иерархическое дерево метаданных: корневой блок `Конфигурации.СистемаЗаказовБК`, свойства `Имя`, `Синоним`, `Комментарий`, `ПринадлежностьОбъекта`, роли, поставщик, версия и другие свойства конфигурации.

## 3. Главная цель

Разработать генератор, который по XML-выгрузке основной конфигурации 1С из `src\cf` и, при наличии, одного расширения из `src\cfe` формирует `Report.txt`, структурно и стилистически близкий к штатному отчету Конфигуратора **“Отчет по конфигурации”**.

## 4. Главный принцип

`Report.txt` должен быть отчетом **по метаданным**, а не отчетом по файлам Git.

Генератор не должен добавлять искусственные технические сведения:

```text
Модуль менеджера
Модуль объекта
Модуль формы
ПутьКФайлу
ФайлBSL
code/main
code/extensions
src/cf
src/cfe
GitCommit внутри каждого объекта
ПоисковыеТеги
Источник: Основная конфигурация
Источник: Расширение
```

Правильное разделение:

```text
Report.txt → дерево метаданных, свойства объектов, вложенные элементы
CODE_PATH  → XML/BSL-файлы для codesearch
```

Поиск по коду остается задачей MCP через `CODE_PATH`. Генератор `Report.txt` не должен дублировать `codesearch`.

## 5. Структура Git-репозитория

Ожидаемая структура:

```text
src\
  cf\
    Configuration.xml
    CommonModules\
    Catalogs\
    Documents\
    InformationRegisters\
    AccumulationRegisters\
    AccountingRegisters\
    Reports\
    DataProcessors\
    Enumerations\
    Subsystems\
    Roles\
    ...

  cfe\
    Configuration.xml
    CommonModules\
    Catalogs\
    Documents\
    InformationRegisters\
    Reports\
    DataProcessors\
    ...
```

Где:

```text
src\cf  — обязательная основная конфигурация
src\cfe — необязательное единственное расширение конфигурации
```

Расширения может не быть. Отсутствие `src\cfe` не является ошибкой, если `extensionRequired=false`.

## 6. Среда выполнения

### 6.1. Текущая среда

MVP запускается на Windows.

Ожидаемая среда:

```text
Windows 10/11 или Windows Server
PowerShell
Git for Windows
Python 3.11+
локальный clone Git-репозитория
```

Примеры путей:

```text
E:\mcp-1c\repos\orders
E:\mcp-1c\staging\orders\current\metadata
E:\mcp-1c\logs
```

### 6.2. Будущая среда

В перспективе генератор должен переехать на Linux без переписывания бизнес-логики.

Требования:

```text
- вся работа с путями через pathlib / path abstraction;
- не использовать Windows-only API;
- не использовать COM/OLE;
- не использовать реестр Windows;
- не использовать Конфигуратор 1С;
- не использовать UI automation;
- не зависеть от PowerShell внутри parser logic;
- принимать все пути из config-файла;
- корректно работать с \ и /;
- корректно работать с кириллицей;
- не зависеть от кодировки консоли Windows.
```

PowerShell допустим только как внешний runner на Windows.

## 7. Язык реализации

Рекомендуемый язык:

```text
Python 3.11+
```

Обоснование:

```text
- кроссплатформенность Windows/Linux;
- удобная работа с XML;
- нормальная работа с Unicode;
- потоковая запись больших файлов;
- запуск из PowerShell, bash, Task Scheduler, cron, GitLab Runner;
- простота автотестирования.
```

## 8. Пользователи

### 8.1. 1С-разработчик

Использует MCP для вопросов:

```text
Какие реквизиты есть у документа Заказ?
Какие ресурсы есть у регистра Фасовки?
Какие свойства у общего модуля?
Какие подсистемы включают документ?
Какая основная форма у справочника?
Какие объекты есть в расширении?
```

### 8.2. DevOps / администратор 1С

Использует генератор в pipeline:

```text
git fetch / pull
  ↓
staging
  ↓
generate-config-report
  ↓
MCP reindex
  ↓
smoke-test
```

### 8.3. Архитектор / аналитик 1С

Использует MCP для анализа состава конфигурации, расширения и структуры метаданных.

## 9. Вне области продукта

Генератор не должен:

```text
- запускать Конфигуратор;
- запускать 1С:Предприятие;
- загружать конфигурацию в служебную ИБ;
- выполнять BSL-код;
- анализировать процедуры и функции BSL;
- строить граф вызовов;
- искать использования реквизитов в коде;
- выполнять git pull/fetch;
- запускать Docker;
- запускать MCP;
- переключать blue-green индексы MCP.
```

Эти задачи относятся к отдельному updater pipeline.

## 10. Входные данные

### 10.1. Config-файл проекта

Пример:

```json
{
  "project": "orders",
  "repoPath": "E:/mcp-1c/repos/orders",
  "mainConfigPath": "src/cf",
  "extensionPath": "src/cfe",
  "extensionRequired": false,
  "outputPath": "E:/mcp-1c/staging/orders/current/metadata",
  "reportFileName": "Report.txt",
  "diagnosticsPath": "E:/mcp-1c/staging/orders/current/diagnostics",
  "logsPath": "E:/mcp-1c/staging/orders/current/logs",
  "encoding": "utf-8"
}
```

### 10.2. Обязательные параметры

```text
project
repoPath
mainConfigPath
outputPath
reportFileName
```

### 10.3. Необязательные параметры

```text
extensionPath
extensionRequired
diagnosticsPath
logsPath
encoding
```

Значения по умолчанию:

```text
extensionPath = "src/cfe"
extensionRequired = false
encoding = "utf-8"
```

Если `src\cfe` отсутствует и `extensionRequired=false`, генератор должен сформировать `Report.txt` только по основной конфигурации.

## 11. Выходные данные

### 11.1. Основной файл

```text
metadata\Report.txt
```

Назначение:

```text
Файл для MCP INDEX_METADATA=true.
```

### 11.2. Диагностика

```text
diagnostics\report-stats.json
diagnostics\report-diagnostics.json
logs\generate-config-report-<timestamp>.log
```

Рекомендуемая структура staging:

```text
staging\
  orders\
    current\
      metadata\
        Report.txt
      diagnostics\
        report-stats.json
        report-diagnostics.json
      logs\
        generate-config-report-20260506-120000.log
```

Диагностические файлы не должны лежать рядом с `Report.txt`, если MCP индексирует все файлы из `METADATA_PATH`.

## 12. Требования к формату `Report.txt`

### 12.1. Общий стиль

Файл должен быть похож на штатный отчет:

```text
- Конфигурации.<ИмяКонфигурации>
	Имя: "<ИмяКонфигурации>"
	Синоним: "<Синоним>"
	Комментарий: ""
	ПринадлежностьОбъекта: "Собственный"
	ОбъектРасширяемойКонфигурации: ""
	...
	- Подсистемы.<ИмяПодсистемы>
		Имя: "<ИмяПодсистемы>"
		Синоним: "<Синоним>"
		Комментарий: ""
		Состав:
			"Документ.Заказ"
			"Справочник.Номенклатура"
```

### 12.2. Форматирование

```text
- иерархия задается табуляцией;
- объект начинается со строки "- <ПолноеИмяОбъекта>";
- свойства выводятся как <ИмяСвойства>: "<Значение>";
- списочные свойства выводятся отдельным блоком;
- вложенные объекты выводятся внутри родительского объекта;
- пустые строковые значения выводятся как "";
- булевы значения выводятся как "Истина" / "Ложь";
- порядок объектов должен быть стабильным;
- порядок свойств должен быть стабильным.
```

### 12.3. Кодировка

Default:

```text
UTF-8 без BOM
```

Параметр `encoding` должен поддерживать:

```text
utf-8
utf-8-sig
cp1251
```

Причина: текущая машина Windows, русские имена объектов и возможные особенности чтения файла MCP.

## 13. Функциональные требования

### FR-001. Чтение основной конфигурации из `src\cf`

Генератор должен читать основную конфигурацию из:

```text
<repoPath>\<mainConfigPath>
```

Default:

```text
src\cf
```

Если каталог не найден, генерация завершается ошибкой.

### FR-002. Опциональное чтение расширения из `src\cfe`

Генератор должен поддерживать одно необязательное расширение из:

```text
<repoPath>\<extensionPath>
```

Default:

```text
src\cfe
```

Если каталог `src\cfe` существует, генератор должен:

```text
1. прочитать XML-выгрузку расширения;
2. определить имя расширения из Configuration.xml;
3. добавить расширение в тот же Report.txt;
4. вывести расширение отдельной корневой секцией;
5. не накладывать расширение на основную конфигурацию.
```

Если каталог `src\cfe` отсутствует:

```text
extensionRequired=false:
  - сформировать Report.txt только по src\cf;
  - записать warning в diagnostics;
  - завершиться с exit code 0 или 1, в зависимости от настройки warningsAsErrors.

extensionRequired=true:
  - не формировать успешный результат;
  - записать error в diagnostics;
  - завершиться с exit code 7.
```

### FR-003. Корневой блок основной конфигурации

Генератор должен сформировать:

```text
- Конфигурации.<ИмяКонфигурации>
```

Минимально поддерживаемые свойства:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
ПоддерживатьСоответствиеОбъектамРасширяемойКонфигурацииПоВнутреннимИдентификаторам
ПрефиксИмен
РежимСовместимостиРасширенияКонфигурации
ОсновнойРежимЗапуска
НазначенияИспользования
ВариантВстроенногоЯзыка
ОсновныеРоли
Поставщик
Версия
АдресКаталогаОбновлений
ОсновнойЯзык
РежимУправленияБлокировкойДанных
РежимСовместимости
ОсновнаяФормаКонстант
```

### FR-004. Корневой блок расширения

Если есть `src\cfe`, генератор должен сформировать отдельный блок:

```text
- Конфигурации.<ИмяРасширения>
```

Не добавлять искусственные поля:

```text
Источник: Расширение
ИмяРасширения
РасширяетОбъект
ПутьРасширения
```

Для связи с расширяемой конфигурацией использовать только штатные свойства, если они есть в XML:

```text
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
ПоддерживатьСоответствиеОбъектамРасширяемойКонфигурацииПоВнутреннимИдентификаторам
ПрефиксИмен
РежимСовместимостиРасширенияКонфигурации
```

### FR-005. Поддержка подсистем

Генератор должен формировать блоки:

```text
- Подсистемы.<Имя>
```

И вложенные подсистемы:

```text
- Подсистемы.<Имя>.Подсистемы.<ИмяВложеннойПодсистемы>
```

Минимальные свойства:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
ВключатьСправкуВСодержание
ВключатьВКомандныйИнтерфейс
ИспользоватьОднуКоманду
Пояснение
Состав
```

`Состав` выводится списком:

```text
Состав:
	"Документ.Заказ"
	"Справочник.Номенклатура"
	"РегистрСведений.Фасовки"
```

### FR-006. Поддержка общих модулей

Генератор должен формировать:

```text
- ОбщиеМодули.<Имя>
```

Минимальные свойства:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
Глобальный
КлиентУправляемоеПриложение
Сервер
ВнешнееСоединение
КлиентОбычноеПриложение
ВызовСервера
Привилегированный
ПовторноеИспользованиеВозвращаемыхЗначений
```

Запрещено добавлять:

```text
Модуль
МодульОбщегоМодуля
ПутьКФайлу
ФайлBSL
```

### FR-007. Поддержка справочников

Генератор должен формировать:

```text
- Справочники.<Имя>
```

Минимальные свойства:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
Иерархический
ВидИерархии
ОграничиватьКоличествоУровней
КоличествоУровней
ГруппыСверху
Владельцы
ИспользованиеПодчинения
ДлинаКода
ДлинаНаименования
ТипКода
ДопустимаяДлинаКода
СерииКодов
КонтрольУникальности
Автонумерация
ОсновноеПредставление
СпособРедактирования
БыстрыйВыбор
СпособВыбора
ВводПоСтроке
ОсновнаяФормаОбъекта
ОсновнаяФормаГруппы
ОсновнаяФормаСписка
ОсновнаяФормаДляВыбора
ОсновнаяФормаДляВыбораГруппы
ДополнительнаяФормаОбъекта
ДополнительнаяФормаГруппы
ДополнительнаяФормаСписка
ДополнительнаяФормаДляВыбора
ДополнительнаяФормаДляВыбораГруппы
ПредставлениеОбъекта
РасширенноеПредставлениеОбъекта
ПредставлениеСписка
РасширенноеПредставлениеСписка
Пояснение
ПолнотекстовыйПоиск
ИсторияДанных
```

Вложенные элементы:

```text
- Справочники.<Имя>.Реквизиты.<Реквизит>
- Справочники.<Имя>.ТабличныеЧасти.<ТЧ>
- Справочники.<Имя>.ТабличныеЧасти.<ТЧ>.Реквизиты.<Реквизит>
- Справочники.<Имя>.Формы.<Форма>
- Справочники.<Имя>.Команды.<Команда>
- Справочники.<Имя>.Макеты.<Макет>
```

### FR-008. Поддержка документов

Генератор должен формировать:

```text
- Документы.<Имя>
```

Минимальные свойства:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
ИспользоватьСтандартныеКоманды
Нумератор
ТипНомера
ДлинаНомера
ДопустимаяДлинаНомера
ПериодичностьНомера
КонтрольУникальности
Автонумерация
СтандартныеРеквизиты
Характеристики
ВводитсяНаОсновании
ОсновнаяФормаОбъекта
ОсновнаяФормаСписка
ОсновнаяФормаДляВыбора
ДополнительнаяФормаОбъекта
ДополнительнаяФормаСписка
ДополнительнаяФормаДляВыбора
Проведение
ОперативноеПроведение
УдалениеДвижений
ЗаписьДвиженийПриПроведении
Движения
ВключатьСправкуВСодержание
ПолнотекстовыйПоиск
ИсторияДанных
ПредставлениеОбъекта
РасширенноеПредставлениеОбъекта
ПредставлениеСписка
РасширенноеПредставлениеСписка
Пояснение
```

Вложенные элементы:

```text
- Документы.<Имя>.Реквизиты.<Реквизит>
- Документы.<Имя>.ТабличныеЧасти.<ТЧ>
- Документы.<Имя>.ТабличныеЧасти.<ТЧ>.Реквизиты.<Реквизит>
- Документы.<Имя>.Формы.<Форма>
- Документы.<Имя>.Команды.<Команда>
- Документы.<Имя>.Макеты.<Макет>
```

### FR-009. Поддержка реквизитов

Генератор должен формировать реквизиты в стиле:

```text
- Документы.Заказ.Реквизиты.Шаблон
	Имя: "Шаблон"
	Синоним: "Шаблон"
	Комментарий: ""
	ПринадлежностьОбъекта: "Собственный"
	ОбъектРасширяемойКонфигурации: ""
	Тип:
		"СправочникСсылка.ШаблоныЗаказов"
	Формат: ""
	ФорматРедактирования: ""
	Подсказка: ""
	ПроверкаЗаполнения: "НеПроверять"
	ПолнотекстовыйПоиск: "Использовать"
	ИсторияДанных: "Использовать"
```

Минимальные свойства:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
Тип
РежимПароля
Формат
ФорматРедактирования
Подсказка
ВыделятьОтрицательные
Маска
МногострочныйРежим
РасширенноеРедактирование
ЗаполнятьИзДанныхЗаполнения
ПроверкаЗаполнения
ВыборГруппИЭлементов
БыстрыйВыбор
СозданиеПриВводе
ФормаВыбора
ИсторияВыбораПриВводе
Индексирование
ПолнотекстовыйПоиск
ИсторияДанных
```

### FR-010. Поддержка табличных частей

Генератор должен формировать:

```text
- Документы.<Имя>.ТабличныеЧасти.<ИмяТЧ>
```

И вложенные реквизиты:

```text
- Документы.<Имя>.ТабличныеЧасти.<ИмяТЧ>.Реквизиты.<ИмяРеквизита>
```

Минимальные свойства табличной части:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
ПроверкаЗаполнения
```

### FR-011. Поддержка регистров сведений

Генератор должен формировать:

```text
- РегистрыСведений.<Имя>
```

Минимальные свойства:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
ИспользоватьСтандартныеКоманды
СпособРедактирования
ОсновнаяФормаЗаписи
ОсновнаяФормаСписка
ДополнительнаяФормаЗаписи
ДополнительнаяФормаСписка
СтандартныеРеквизиты
ПериодичностьРегистраСведений
РежимЗаписи
ОсновнойОтборПоПериоду
ВключатьСправкуВСодержание
РежимУправленияБлокировкойДанных
ПолнотекстовыйПоиск
РазрешитьИтогиСрезПервых
РазрешитьИтогиСрезПоследних
ПредставлениеЗаписи
РасширенноеПредставлениеЗаписи
ПредставлениеСписка
РасширенноеПредставлениеСписка
Пояснение
ИсторияДанных
```

Вложенные элементы:

```text
- РегистрыСведений.<Имя>.Измерения.<Измерение>
- РегистрыСведений.<Имя>.Ресурсы.<Ресурс>
- РегистрыСведений.<Имя>.Реквизиты.<Реквизит>
- РегистрыСведений.<Имя>.Формы.<Форма>
- РегистрыСведений.<Имя>.Команды.<Команда>
- РегистрыСведений.<Имя>.Макеты.<Макет>
```

### FR-012. Поддержка регистров накопления

Генератор должен формировать:

```text
- РегистрыНакопления.<Имя>
```

Минимальные свойства:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
ВидРегистра
ИспользоватьСтандартныеКоманды
ОсновнаяФормаСписка
ОсновнаяФормаЗаписи
ВключатьСправкуВСодержание
РежимУправленияБлокировкойДанных
ПолнотекстовыйПоиск
ИсторияДанных
```

Вложенные элементы:

```text
Измерения
Ресурсы
Реквизиты
Формы
Команды
Макеты
```

### FR-013. Поддержка регистров бухгалтерии и расчета

Генератор должен формировать:

```text
- РегистрыБухгалтерии.<Имя>
- РегистрыРасчета.<Имя>
```

Поддерживать доступные в XML свойства в штатном стиле.

Вложенные элементы:

```text
Измерения
Ресурсы
Реквизиты
Формы
Команды
Макеты
Перерасчеты
```

### FR-014. Поддержка отчетов и обработок

Генератор должен формировать:

```text
- Отчеты.<Имя>
- Обработки.<Имя>
```

Минимальные свойства:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
ИспользоватьСтандартныеКоманды
ОсновнаяФорма
ДополнительнаяФорма
ОсновнаяСхемаКомпоновкиДанных
ОсновнаяФормаНастроек
ДополнительнаяФормаНастроек
ОсновнаяФормаВарианта
ХранилищеВариантов
ХранилищеНастроек
ВключатьСправкуВСодержание
РасширенноеПредставление
Пояснение
```

Вложенные элементы:

```text
Формы
Команды
Макеты
Реквизиты
ТабличныеЧасти
```

### FR-015. Поддержка форм

Генератор должен формировать:

```text
- Документы.<Имя>.Формы.<ИмяФормы>
- Справочники.<Имя>.Формы.<ИмяФормы>
- Отчеты.<Имя>.Формы.<ИмяФормы>
- Обработки.<Имя>.Формы.<ИмяФормы>
- ОбщиеФормы.<ИмяФормы>
```

Минимальные свойства:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
ТипФормы
ВключатьСправкуВСодержание
НазначенияИспользования
РасширенноеПредставление
```

Запрещено добавлять:

```text
МодульФормы
ПутьКМодулюФормы
ФайлФормы
```

### FR-016. Поддержка макетов

Генератор должен формировать:

```text
- Документы.<Имя>.Макеты.<ИмяМакета>
- Отчеты.<Имя>.Макеты.<ИмяМакета>
- Обработки.<Имя>.Макеты.<ИмяМакета>
- ОбщиеМакеты.<ИмяМакета>
```

Минимальные свойства:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
ТипМакета
```

### FR-017. Поддержка команд

Генератор должен формировать:

```text
- Документы.<Имя>.Команды.<ИмяКоманды>
- Справочники.<Имя>.Команды.<ИмяКоманды>
- Отчеты.<Имя>.Команды.<ИмяКоманды>
- Обработки.<Имя>.Команды.<ИмяКоманды>
- ОбщиеКоманды.<ИмяКоманды>
```

Поддерживать свойства, доступные в XML, в штатном стиле.

### FR-018. Поддержка перечислений

Генератор должен формировать:

```text
- Перечисления.<Имя>
- Перечисления.<Имя>.Значения.<ИмяЗначения>
```

Минимальные свойства:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
```

### FR-019. Поддержка ролей

Генератор должен формировать:

```text
- Роли.<Имя>
```

В MVP достаточно базовых свойств:

```text
Имя
Синоним
Комментарий
ПринадлежностьОбъекта
ОбъектРасширяемойКонфигурации
```

Подробные права роли можно вынести на следующий этап.

### FR-020. Поддержка прочих объектов метаданных

Генератор должен по возможности поддерживать:

```text
Константы
ПланыОбмена
ПланыВидовХарактеристик
ПланыСчетов
ПланыВидовРасчета
БизнесПроцессы
Задачи
ЖурналыДокументов
ОбщиеФормы
ОбщиеКоманды
ОбщиеМакеты
ОбщиеКартинки
ОбщиеРеквизиты
ОпределяемыеТипы
ФункциональныеОпции
ПараметрыФункциональныхОпций
ПараметрыСеанса
ПодпискиНаСобытия
РегламентныеЗадания
WebСервисы
HTTPСервисы
ПакетыXDTO
WSСсылки
Стили
Языки
КритерииОтбора
Последовательности
```

Для неподдержанного типа объекта генератор должен вывести минимум:

```text
- <Тип>.<Имя>
	Имя: "<Имя>"
	Синоним: "<Синоним>"
	Комментарий: "<Комментарий>"
	ПринадлежностьОбъекта: "<...>"
	ОбъектРасширяемойКонфигурации: "<...>"
```

И добавить warning в diagnostics.

## 14. Требования по расширению `src\cfe`

### FR-021. Расширение необязательно

В MVP поддерживается не более одного расширения:

```text
src\cfe
```

Расширение может отсутствовать.

Возможные режимы:

```text
1. Конфигурация без расширения:
   src\cf есть
   src\cfe отсутствует
   extensionRequired=false
   → Report.txt содержит только - Конфигурации.<ИмяОсновнойКонфигурации>

2. Конфигурация с расширением:
   src\cf есть
   src\cfe есть
   → Report.txt содержит:
      - Конфигурации.<ИмяОсновнойКонфигурации>
      - Конфигурации.<ИмяРасширения>

3. Расширение обязательно, но отсутствует:
   src\cf есть
   src\cfe отсутствует
   extensionRequired=true
   → ошибка exit code 7
```

### FR-022. Расширение выводится отдельной корневой секцией

Правильно:

```text
- Конфигурации.<ИмяОсновнойКонфигурации>
	...

- Конфигурации.<ИмяРасширения>
	...
```

Неправильно:

```text
- Конфигурации.<ИмяОсновнойКонфигурации>
	...
	- Расширение.<ИмяРасширения>
```

### FR-023. Без merged-view

Генератор не должен объединять объекты основной конфигурации и расширения в итоговый объект.

Неправильно:

```text
Документ.Заказ = основной Документ.Заказ + изменения расширения
```

Правильно:

```text
Основная конфигурация:
	- Документы.Заказ

Расширение:
	- Документы.Заказ
		ПринадлежностьОбъекта: "Заимствованный"
		ОбъектРасширяемойКонфигурации: "<если есть в XML>"
```

## 15. Нефункциональные требования

### NFR-001. Максимальная близость к штатному отчету

Приоритеты:

```text
1. Не добавлять лишнего.
2. Сохранять штатные имена свойств.
3. Сохранять иерархию.
4. Сохранять стиль списков.
5. Сохранять кавычки значений.
6. Сохранять пустые значения как "".
7. Не выводить файловые пути.
```

### NFR-002. Детерминированность

Одна и та же Git-выгрузка должна давать одинаковый `Report.txt`.

Требования:

```text
- стабильная сортировка объектов;
- стабильный порядок свойств;
- стабильный порядок вложенных элементов;
- отсутствие timestamp внутри Report.txt;
- отсутствие commit SHA внутри Report.txt;
- отсутствие случайных GUID, если они не являются штатным свойством.
```

### NFR-003. Производительность

Генератор должен обрабатывать крупные конфигурации.

Ориентиры:

```text
- до 100 000 файлов;
- Report.txt размером до нескольких сотен МБ;
- потоковая запись результата;
- не держать весь отчет в памяти;
- время генерации должно быть существенно меньше времени индексации MCP.
```

### NFR-004. Кроссплатформенность

Несмотря на текущий запуск на Windows, код должен быть готов к Linux.

Требования:

```text
- Python pathlib для путей;
- отсутствие hardcoded backslash;
- отсутствие PowerShell внутри parser logic;
- отсутствие cmd.exe-зависимостей;
- отсутствие COM/OLE;
- отсутствие Windows registry;
- отсутствие зависимости от текущей кодировки консоли;
- все параметры через config/CLI.
```

### NFR-005. Windows-friendly behavior

На Windows генератор должен:

```text
- корректно работать с путями вида E:\mcp-1c\repos\orders;
- корректно работать с кириллицей в путях и файлах;
- писать лог в файл, а не только stdout;
- возвращать exit code для Task Scheduler;
- не требовать интерактивной сессии пользователя;
- не открывать GUI;
- не блокировать репозиторий для git pull после завершения.
```

### NFR-006. Linux-ready behavior

На Linux генератор должен:

```text
- запускаться тем же CLI;
- принимать пути вида /opt/mcp-1c/repos/orders;
- не требовать изменения формата config, кроме путей;
- писать stdout/stderr корректно для cron/systemd;
- использовать те же exit codes.
```

### NFR-007. Отказоустойчивость

Один поврежденный XML-файл не должен полностью останавливать генерацию, если можно продолжить.

Поведение:

```text
- ошибка фиксируется в report-diagnostics.json;
- проблемный объект пропускается или выводится частично;
- генерация продолжается;
- итоговый exit code зависит от критичности.
```

### NFR-008. Безопасность

Генератор не должен:

```text
- исполнять BSL;
- исполнять команды из репозитория;
- модифицировать Git-репозиторий;
- подключаться к базам 1С;
- хранить учетные данные;
- отправлять данные наружу.
```

## 16. CLI

### 16.1. Запуск на Windows

```powershell
python E:\mcp-1c\tools\generate-config-report\generate_config_report.py `
  --config E:\mcp-1c\projects\orders.json
```

### 16.2. Запуск на Linux в будущем

```bash
python3 /opt/mcp-1c/tools/generate-config-report/generate_config_report.py \
  --config /opt/mcp-1c/projects/orders.json
```

### 16.3. CLI-параметры

Минимально:

```text
--config <path>       путь к config-файлу
--verbose             расширенный лог
--strict              завершать ошибкой при warning-классах, заданных в config
--dry-run             проверить входные данные без записи Report.txt
```

## 17. Диагностика

### 17.1. `report-stats.json`

Пример:

```json
{
  "project": "orders",
  "mainConfigPath": "src/cf",
  "extensionPath": "src/cfe",
  "extensionFound": false,
  "extensionRequired": false,
  "generatedAt": "2026-05-06T12:00:00",
  "mainConfigurationObjects": 12500,
  "extensionObjects": 0,
  "objectsByType": {
    "Documents": 120,
    "Catalogs": 180,
    "CommonModules": 900,
    "InformationRegisters": 240
  },
  "warnings": 1,
  "errors": 0
}
```

`generatedAt` допустим в diagnostics, но не в `Report.txt`.

### 17.2. `report-diagnostics.json`

Должен содержать:

```text
- не найденные каталоги;
- отсутствие src/cfe, если extensionRequired=false;
- ошибку отсутствия src/cfe, если extensionRequired=true;
- XML-файлы, которые не удалось прочитать;
- XML-файлы, которые не удалось распарсить;
- объекты без имени;
- объекты без ожидаемого корневого элемента;
- свойства, которые не удалось преобразовать в строку;
- неподдержанные типы объектов;
- пропущенные вложенные элементы;
- ошибки чтения расширения.
```

## 18. Exit codes

```text
0 — Report.txt успешно сформирован, критичных ошибок нет.
1 — Report.txt сформирован, есть warnings.
2 — Report.txt не сформирован из-за критической ошибки.
3 — неверные параметры запуска.
4 — не найден путь основной конфигурации src/cf.
5 — не найдено ни одного объекта метаданных.
6 — ошибка записи Report.txt.
7 — extensionRequired=true, но src/cfe не найден.
8 — ошибка чтения config-файла.
```

Если `extensionRequired=false`, отсутствие `src\cfe` не является ошибкой.

## 19. MVP

### 19.1. В MVP входит

```text
1. Чтение основной конфигурации из src/cf.
2. Чтение одного необязательного расширения из src/cfe, если каталог существует.
3. Генерация одного Report.txt.
4. Поддержка:
   - корень конфигурации;
   - корень расширения, если расширение есть;
   - подсистемы;
   - общие модули;
   - справочники;
   - документы;
   - регистры сведений;
   - регистры накопления;
   - отчеты;
   - обработки;
   - перечисления;
   - формы;
   - макеты;
   - реквизиты;
   - табличные части;
   - измерения;
   - ресурсы;
   - команды.
5. Генерация report-stats.json.
6. Генерация report-diagnostics.json.
7. Логирование в файл.
8. Потоковая запись Report.txt.
9. Стабильный порядок вывода.
10. Работа на Windows.
11. Кодовая база без Windows-only зависимостей.
```

### 19.2. В MVP не входит

```text
1. Поддержка нескольких расширений.
2. Полное покрытие всех типов метаданных.
3. Полное совпадение со штатным отчетом до каждого свойства.
4. Наложение расширения на основную конфигурацию.
5. Анализ BSL.
6. Вывод путей к BSL.
7. Вывод "модуль менеджера", "модуль объекта", "модуль формы".
8. Docker/update pipeline.
9. Git pull/fetch.
10. Управление MCP.
11. Linux deployment.
```

Linux deployment не входит в MVP, но архитектура генератора должна быть готова к переносу.

## 20. Критерии приемки

### AC-001. Формируется `Report.txt`

После запуска создается:

```text
metadata\Report.txt
```

Файл не пустой и содержит:

```text
- Конфигурации.<ИмяОсновнойКонфигурации>
```

### AC-002. Формат похож на штатный отчет

Объекты выводятся в формате:

```text
- <ПолноеИмяОбъекта>
	Имя: "<Имя>"
	Синоним: "<Синоним>"
	Комментарий: "<Комментарий>"
```

### AC-003. Нет искусственных модульных полей

В `Report.txt` не должно быть искусственно добавленных строк:

```text
Модуль менеджера:
Модуль объекта:
Модуль формы:
ПутьКФайлу:
ФайлBSL:
code/main/
code/extensions/
src/cf
src/cfe
```

### AC-004. Подсистемы содержат состав

Для подсистем выводится:

```text
Состав:
	"Документ.Заказ"
	"Справочник.Номенклатура"
```

### AC-005. Общие модули содержат свойства исполнения

Для общих модулей выводятся:

```text
Глобальный
КлиентУправляемоеПриложение
Сервер
ВнешнееСоединение
КлиентОбычноеПриложение
ВызовСервера
Привилегированный
ПовторноеИспользованиеВозвращаемыхЗначений
```

### AC-006. Документы содержат реквизиты и табличные части

Для документа должны выводиться:

```text
- Документы.<Имя>.Реквизиты.<ИмяРеквизита>
- Документы.<Имя>.ТабличныеЧасти.<ИмяТЧ>
```

### AC-007. Регистры содержат измерения, ресурсы и реквизиты

Для регистра должны выводиться:

```text
- РегистрыСведений.<Имя>.Измерения.<Имя>
- РегистрыСведений.<Имя>.Ресурсы.<Имя>
- РегистрыСведений.<Имя>.Реквизиты.<Имя>
```

### AC-008. Отчеты и обработки содержат формы и макеты

Для отчетов и обработок должны выводиться:

```text
- Отчеты.<Имя>.Формы.<ИмяФормы>
- Отчеты.<Имя>.Макеты.<ИмяМакета>
```

### AC-009. Расширение из `src\cfe` опционально

Если каталог `src\cfe` существует, в `Report.txt` должна появиться отдельная корневая секция:

```text
- Конфигурации.<ИмяРасширения>
```

Если каталог `src\cfe` отсутствует и `extensionRequired=false`, генератор должен успешно сформировать `Report.txt` только по основной конфигурации.

### AC-010. Расширение не объединяется с основной конфигурацией

Не должно быть merged-view.

Объекты основной конфигурации и расширения выводятся в своих корневых секциях.

### AC-011. Результат индексируется MCP

После передачи `Report.txt` в MCP с `INDEX_METADATA=true` поиск должен находить:

```text
Конфигурация
Подсистема
Общий модуль
Документ
Справочник
Регистр сведений
Реквизит
Форма
Макет
Объект расширения, если расширение есть
```

### AC-012. Повторяемость

Два запуска на одной и той же Git-выгрузке дают одинаковый `Report.txt`.

### AC-013. Запуск из Windows Task Scheduler

Генератор должен корректно завершаться с exit code и писать лог в файл при запуске без интерактивной сессии пользователя.

### AC-014. Path portability

Один и тот же код должен работать с путями:

```text
E:\mcp-1c\repos\orders
```

и в будущем:

```text
/opt/mcp-1c/repos/orders
```

без изменения логики генератора.

## 21. Контроль качества

Для проверки качества использовать эталонный файл:

```text
ОтчетПоКонфигурации.txt
```

Проверка не бинарным diff, а по критериям:

```text
1. Совпадает общий стиль.
2. Совпадает иерархия.
3. Совпадают штатные имена свойств.
4. Нет нештатных технических секций.
5. Основные объекты присутствуют.
6. Вложенные объекты присутствуют.
7. Значения свойств выводятся в кавычках.
8. Пустые значения выводятся как "".
9. Списки выводятся блоками.
10. Если расширение есть, оно выводится отдельной корневой секцией.
11. Если расширения нет, отчет по основной конфигурации все равно формируется успешно.
```

## 22. Риски

### Риск 1. XML-выгрузка не содержит часть свойств штатного отчета

Решение:

```text
Если свойства нет в XML, генератор его не выдумывает.
Допустимо пропустить свойство.
Запрещено заменять его искусственным техническим полем.
```

### Риск 2. Формат XML отличается для разных типов объектов

Решение:

```text
Делать parser итерационно.
Для неподдержанных объектов выводить базовый блок.
Все пропуски фиксировать в diagnostics.
```

### Риск 3. Расширение сложно представить как штатный отчет

Решение:

```text
Не делать наложение.
Выводить src/cfe отдельной корневой секцией.
Использовать штатные свойства принадлежности.
Если src/cfe отсутствует, пропускать расширение без ошибки при extensionRequired=false.
```

### Риск 4. Windows-специфика попадет в код

Решение:

```text
Вся работа с путями через pathlib.
PowerShell только как внешний runner.
Все настройки через JSON config.
Автотесты путей Windows/Linux.
```

## 23. Итоговая формулировка задачи

Разработать генератор `generate-config-report`, который по XML-выгрузке основной конфигурации 1С из `src\cf` и, при наличии, одного расширения из `src\cfe` формирует `Report.txt`, структурно и стилистически близкий к штатному отчету Конфигуратора **“Отчет по конфигурации”**.

Расширение является необязательным. Если `src\cfe` отсутствует и `extensionRequired=false`, генератор должен сформировать `Report.txt` только по основной конфигурации.

Генератор должен выводить дерево метаданных, свойства объектов и вложенные элементы. Он не должен добавлять сведения о физических файлах, BSL-модулях, путях в Git или иных данных, отсутствующих в штатном отчете.

Основная среда выполнения MVP — Windows. Код должен быть написан кроссплатформенно, чтобы в дальнейшем без переписывания логики перенести выполнение на Linux.

`Report.txt` используется только для `metadatasearch` в MCP. Поиск по BSL-коду остается задачей `codesearch` через `CODE_PATH`.
